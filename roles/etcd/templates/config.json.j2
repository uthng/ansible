{
    "CN": "{{ ansible_hostname }}",
    "hosts": [
        "{{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}",
        "{{ hostvars[inventory_hostname].ansible_host }}",
        "{{ ansible_hostname }}"
    ],
    "key": {
        "algo": "ecdsa",
        "size": 256
    },
    "names": [
        {
            "C": "FR",
            "L": "ILE DE FRANCE",
            "ST": "PARIS"
        }
    ]
}

